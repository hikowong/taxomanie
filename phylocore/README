PhyloCore
=========

This library provides classes and methods to manipulate phylogenetic trees.

Install
-------

$ python setup.py install

Please, after installing phylocore, lanch the `preproc.py` tools in order
to build the NCBI database. Then, put the file `taxonomy.csv` generated in
`/usr/share/phyloexplorer/data` path.
